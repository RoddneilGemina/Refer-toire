{
  "name": "refertoire-workspace",
  "version": "1.1.1",
  "private": true,
  "description": "Refertoire workspace with web demo and standalone APK",
  "scripts": {
    "start": "npm --prefix web start",
    "web": "npm --prefix web run web",
    "android": "npm --prefix web run android",
    "ios": "npm --prefix web run ios",
    "build:web": "npm --prefix web run build:web"
  }
}
